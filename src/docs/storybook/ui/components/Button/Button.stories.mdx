import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs/blocks";

import Button from "../../../../../ui/components/Button";
import { ComponentProps } from "../../../../../ui/components/Button/types";

<Meta
	title="UI Kit/Button"
	component={Button}
	argTypes={{
		textColor: { control: 'color' },
		onClick: { action: 'clicked' }
	}}
/>

# Button

Button component based on [Material-UI button](https://material-ui.com/components/buttons/).

## Sizes

<Canvas>
  <Story name="size-small" args={{size: 'small', children: 'Small button'}}>{args => <Button {...args} />}</Story>
  <Story name="size-medium" args={{size: 'medium', children: 'Medium button'}}>{args => <Button {...args} />}</Story>
  <Story name="size-large" args={{size: 'large', children: 'Large button'}}>{args => <Button {...args} />}</Story>
</Canvas>

## Types

<Canvas>
  <Story name="type-text" args={{variant: 'text', children: 'Text button'}}>{args => <Button {...args} />}</Story>
  <Story name="type-outlined" args={{variant: 'outlined', children: 'Outlined button'}}>{args => <Button {...args} />}</Story>
  <Story name="type-contained" args={{variant: 'contained', children: 'Contained button'}}>{args => <Button {...args} />}</Story>
</Canvas>

<ArgsTable of={Button} />
